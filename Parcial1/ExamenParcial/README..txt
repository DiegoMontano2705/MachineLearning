*Se deben tener los archivos de los datasets en la misma direccion que el codigo fuente*
Correr el código desde terminal: 
python examenParcial1.py
En caso de no tener las librerias necesarias instalarlas usando:
pip install numpy
pip install pandas
pip install scikit-learn